
#include<stdio.h>
#include<stdlib.h>

//inserting a node at the beginning of a linked list
/*
struct node{
int x;
struct node *next;
}
struct node *first=NULL,
struct node *new_node;
*/

/*
this:
(*new_node).value=10;
makes:
new_node -> value=10;
*/

/*

*/

struct node {
    new_node->value=n;
    new_node->next=list;
};

struct node {
    int value;
    struct node *next;
};

void main(){ //I FUCKING DONT CARE I STOPPED
struct node *add_to_list(struct node *list, n);
first=*add_to_list(first, 10)

/*
new_node=malloc(sizeof(struct node));
if(new_node==NULL){
    printf("memory allocation failed.");
    return;
}
    new_node->value=n;
    new_node->next=first;
    return;
*/
}
