#include<stdio.h>

void main(){
    /*int i,j=0;
    char x[10];
    printf("Enter some bullshit: ");
    //scanf("%s",&x);
    gets(x);
    for (i=0;i<10;i++){
        printf("%s",x[j]);
        j++;
    }
    */

    /*
    //trying to make a thing where you input a string and it outputs any string but spaced
    int len;
    char scan[len];
    printf("Input some shit: ");
    scanf("%s",scan);
    len = strlen(scan);
    printf("DEBUG: Size of: %d\n",len);
    printf("DEBUG: String: %s\n",scan);
    for (int i=0;i<len;i++){
        //printf("%d",i);
        printf("%c ",scan[i]);
    }
    */

    for(int l=0;l=1;l=2){
        printf("balls");
    }

    while(5)
        printf("something");
}
